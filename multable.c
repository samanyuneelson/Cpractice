#include <stdio.h>
//C program to read an integer and print its multiplication table.
int main(void)
{
  int a,mul,pro;
  printf("enter number:");
  scanf ("%d", &a);
  for (mul = 1; mul <= 10; mul++)
}
